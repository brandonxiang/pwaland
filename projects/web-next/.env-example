VITE_APP_ENV=test
VITE_APP_VISUALIZER=false

# API proxy target for Vite dev server
# Default: http://localhost:3000 (local Fastify server)
# Set to production URL to use prod data without running a local server:
# VITE_API_PROXY_TARGET=https://pwaland.brandonxiang.top
